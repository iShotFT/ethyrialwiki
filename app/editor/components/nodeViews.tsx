import * as React from "react";
import MapComponent from "./MapComponent";

// Map node types to React components
const nodeToComponent = {
  map_block: MapComponent,
};

export default nodeToComponent;
